<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ec.concurrency.dao.ProductDao">
    <select id="findById" parameterType="long" resultType="com.ec.concurrency.entity.Product">
      select * from f3469.shop where id = #{value}
    </select>
    </mapper>